{
  "version": "0.0.0",
  "name": "AngelaMin",
  "description": "Start Angela Projekt",
  "devDependencies": {
    "http-server": "^0.6.1"
  },
  "scripts": {
    "prestart": "npm install",    
    "start": "http-server -a 0.0.0.0 -p 8002"
  }
}
